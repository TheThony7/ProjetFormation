{% extends 'base.html.twig' %}

{% block title %}
	{{ oneProjet.nom }}
{% endblock %}

{% block body %}
	<h1 >{{ oneProjet.nom }}</h1>
	<p class="fs-4 text-white">{{ oneProjet.description }}</p>
	
	

	<hr class="text-white">

	<div class="row pt-3 pb-3">
		<div class="card p-4 rounded-3">
			<p>{{ oneProjet.categorie }}</p>
		</div>
	</div>

	<hr class="text-white">

	{# <div class="col-md-12 text-center">
		<a class="btn btn-warning" href={{ path('notes') }}>Revenir à toutes les projets</a>
		<a class="btn btn-primary" href={{ path('update_note', { id: oneProjet.id} ) }}>Modifier la note</a>
	</div> #}

{% endblock %}